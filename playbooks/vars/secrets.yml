---
# =============================================================================
# Secrets du projet (J5)
# EN PRODUCTION : chiffrer ce fichier avec Ansible Vault avant tout commit :
#   ansible-vault encrypt playbooks/vars/secrets.yml
# Puis exécuter les playbooks avec --ask-vault-pass (ou --vault-password-file).
# Cette version en clair n'est qu'un exemple pédagogique.
# =============================================================================
vault_taskflow_api_token: "s3cr3t-token-de-demonstration"
